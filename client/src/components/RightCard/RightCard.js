import React, { Fragment } from "react";
import "./RightCard.scss";

const RightCard = () => {
  return (
    <Fragment>
      <div className="right-card">
        <h3>
          Working : The input will be passed onto the SQL database, after which
          the data will added into the Database Tables using appropriate SQL
          Queries.
        </h3>
      </div>
    </Fragment>
  );
};

export default RightCard;
